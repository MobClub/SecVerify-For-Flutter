name: secverify_plugin
description: The operator gateway takes the number and verifies the mobile phone number in one second.
version: 1.30.10
homepage: https://www.mob.com/mobService/secverify

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.12.13+hotfix.6"

dependencies:
  flutter:
    sdk: flutter
#  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
#  build_runner:
#  json_serializable:

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.mob.flutter.secverify
        pluginClass: SecverifyPlugin
      ios:
        pluginClass: SecverifyPlugin